# Hall RPM mapping experiment

`mapping_hall_RPM.py` measures magnetic-field rotation with an MCP2221 and an
MLX90393 while commanding the Arduino motor controller.

This pipeline is scientifically useful but remains experimental in v1.0.2.
The related prototypes are retained under `firmware/hall_sensor/`.
